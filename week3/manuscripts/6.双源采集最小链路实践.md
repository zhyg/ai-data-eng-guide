大家好，今天我们开始讲解第三周的实践课程。本次课程将不再讨论抽象概念，而是实际操作，验证我们现有的最小采集闭环是否能够顺利运行。 实验旨在验证Copilot报告的采集基线，包括合约、清单文件的有效性以及Ticket和文档链路的可解释性。此外，还将检查Dagster资产图能否准确表达当前的采集资产关系，并基于这些证据进行恢复判断。整个实验分为4到5个步骤。 在执行命令之前，我们会先找到仓库对象。回顾一下，我们的数据契约文件位于contract下，而控制数据下的media x文件包含了多个face文件。接下来我们将运行脚本，包括文档采集和统计相关的batchfill文件等。
我们将控制输入面与生成的接口文件及ticket脚本进行衔接。以vibe coding的方式实现，代码的主要功能是对接收到的工单进行校验并入库。 具体来说，该过程涉及铜层和银层的数据处理，通过异步批次作用生成预期报告。输入源为一个source和JSON文件，经过contract教练和manifest的校验后，最终写入PostgreSQL数据库中的铜层。 接下来介绍Dagster，它负责任务调度。第二周讲解的内容主要是契约验证，包括静态schema验证和语义应用验证，确保数据结构正确且符合业务逻辑。
第三部分介绍了一个新内容：资产分析。我们将其与dark store组件关联起来，处理资产文件。这里有两个关键点：一个是asset的Python代码，另一个是定义在外部的appliance in tion。 我们的定义非常简短，它实际上是Dagster项目的装配入口。通过使用Dagster的一些接口文件，我们可以实现与Dagster的连接。此外，我们还需要定义资产及其依赖关系，明确每个资产的具体功能。 这些定义和配置类似于编排功能，通过编排资产来形成一个整体。在后续的操作中，我会在界面上详细展示具体步骤。底层代码部分，有兴趣的朋友可以自行深入研究。
我们现在位于项目目录下，我将在卖分支上进行操作。首先，建议大家复制界面，以防环境出现问题。接下来，我们将使用compose文件启动系统，并查看是否有相关文件。 我们通过compose文件启动系统，并进行后台构建。本地已经下载了镜像，因此启动速度较快。接下来，我们将验证一些接口是否正常工作，例如使用curl命令检查8000端口上的fastAPI接口。
这个配置没有问题。801是我们服务的端口，不是tool端口。接下来，我们看一下其他组件，比如media l。三千是dark st。今天主要讲解dark st，其他组件就不细看了。 首先，我们运行contract的baseline，从总体上检查数据契约是否有问题。这将作为我们的准入标准。我们可能会运行ingest或compose工具。 我们将使用一个虚拟的容器devbox来进行Python测试，类似于CICD测试。接下来，我们会运行Dagster和contract的所有相关测试。
所有测试都已通过，接下来我们将继续进行下一步。具体的测试细节可以参考第二周的内容。这表明我们的 contract base line 已经成功赞助，我们可以继续运行下面的 c load。 我们将在本地环境中运行 manifest 和 c load。使用的环境变量是本地配置的。接下来，我们将运行一个容器，使用模块化脚本来执行集成任务。这个 loader 是一个 Python 模块，用于处理 seed manifest 和 seed loader。manifest 的目录位于 Data 上方。
我们先完整地运行一遍。为了留痕，我们将生成一个报告。这次我们使用week3文件来处理node的small label report。dry run模式下，结果会显示batchID。我们有四个manifest可用，总共涉及12个资产。 这12个资产中，有10个通过了检查，1个发出警告，1个被隔离。这些资产分为四部分，第一部分是gateway边缘计算相关的，其中三个通过了检查。接下来是工单同步和结构化数据部分，有两个通过了检查。 现在我们来看更详细的报告。报告将提供更多信息，包括每个部分的具体内容。这样可以更好地了解整个过程的结果和细节。
我们重点查看报告中的警告或隔离情况及其原因。报告中包含运行数量和隔离数量等结构化数据。例如，如果PI扫描未运行，则会显示相关警告。这些信息有助于我们理解报告的实际用途。 接下来，我们将进行工单结构化数据的采集。首先使用工单模拟器生成一个JSON文件，这一步是为了确保数据输入的可复现性。实验中采用合成数据，以保证每个人都能稳定地复现结果。 最后，我们将执行票证的导入测试。重要的是观察系统如何处理JSON文件，并复用票证合同来统计有效票证总数。这个过程不关注是否写入数据库，而是验证数据处理的正确性。
接下来，我们将讨论一些重要的统计量，并解释为什么必须认真对待batch id。首先，我们进行一次数据合成。我们将使用一个合成生成器的代码，这部分内容请自行查阅。 我们将运行一个临时容器来生成合成数据。使用的生成器是ticket模拟器。我们将生成大约一千条记录，并输出到指定位置。这些数据将通过并行计算的方式生成。完成后，我们可以查看生成的数据。
确认make show的数据已经准备好。接下来进行数据采集，将生成的数据从目录中采集到数据库中。这些数据可能通过PostgreSQL存储。 先排除干扰因素，再运行相关测试。考虑将重复输入的部分封装成函数以简化操作。我们将运行tickets personal，并检查其输入是否正确。
我们不需要使用 Data，而是继续使用 tips。这是 level 01 的内容。接下来，我们将为数据添加一个 batchID，这是非常重要的一步。为了保持一致性，我们将文件命名为 Number week 03 ticket 0，以便留痕。 我们现在运行的是 dry run 模式，而不是生产模式。我们需要将结果作为证据留痕，并将其放在 week 03 文件夹下。我们将使用 ticket ingest 这个名字来进行留痕。 现在我们来运行这个文件并查看结果。这个地方应该是一个运行的配置文件或属性文件。请检查一下是否已经成功处理了 1000 条数据。
输入包括总数和有效数，无效数为零。实际插入数据库的操作并未执行，仅排除了风险。实际插入将在后续周内验证，可能在第四周进行。 关于ingest ticket的Python文件，请大家自行查看代码。接下来我们将运行文档类型的dry run。文档链路与工单链路不同，更强调原始文件，包含source figure、print、licence等元素。 此外，我们还有一个未查看的报告，它比统计分析更丰富，包含类型、run ID、生成时间、batch ID等信息。如果推荐步骤不通过，则无法继续。
接下来我们运行代码，这一步很容易出错。关于代码的具体细节，建议大家自行查阅相关文档。在AI时代，直接讲解代码的意义不大，因为很多情况下代码由AI生成。我们需要关注的是工单的处理。 首先验证manifest文件，然后进行dry run。同样，我们需要为操作指定一个Batch ID，这是文档类型的ID。我们将进行一次搬迁操作，并做好记录，这是一种良好的工程习惯。
报告JSON中总共有四个文档。其中，200100、2003004被跳过（skipped），这体现了流转的重要性。这些跳过并不等于错误，因为在dry run模式下，数据没有实际写入数据库。 在dry run模式下，ticket和document的两条链路都进行了dry run。这意味着没有真实的数据写入操作。接下来，我们将观察S对象如何从脚本视角转变为资产。 我们使用Dagster进行编排，它帮助我们管理和执行整个工作流。通过Dagster，我们可以更好地理解数据流转和处理过程。
Dagster可以将数据工程的任务通过图的方式串联起来，并支持自动化运行和调度。我们用三周时间学习了这些复杂功能。现在，让我们检查一下采集链路是否已经建立。 从Python脚本与Dagster对接来看，首先通过manifest进行输入控制，然后分为两条路径：一条是docker的assistants，另一条是ticket system工单系统。可以看到，这些流程运行后都显示为绿色，表明它们能够正常运行。 目前跑通的部分包括seed manifest、ticket处理以及no doctor assistant。接下来我们将通过Dagster编排所有链路，形成一个完整的Pipeline。此外，数据湖的相关内容将在第四课中详细讲解。
这已经是物化的状态，但有些可能还未物化。这意味着它们尚未进入我们的生产产品。根据定义，这些可以作为资产进行编排，并且应该有运行记录。此外，还有 backfill 和 Rush 调度，每次运行时都会生成一个 run ID。 Dagster 作为我们数据编排工具的组件，支持自动化。目前，为了公司的进度，我们只能手动操作。这周的结果已经得到了验证，所有流程都已跑通且无误。从第四周开始，我们将进入实际生产，可以看到实际数据。 大家可以在群内讨论问题。相关文件包括 asset python 和 deform 定义的 python。本周的内容就到这里结束。
随着工程的深入，组件的效果会越来越明显。为了帮助大家理解每周的代码结构，我会详细说明并上传相关文档。第三周五的run book中包含了详细的代码架构，以帮助大家理解整体结构。 每周我们都会更新代码结构图，通过链的关系和文件、字段级别的详细说明来帮助大家理解。如果有任何问题，欢迎大家在群内提问。
