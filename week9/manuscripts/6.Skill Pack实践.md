大家好，今天我们正式进入第9周的实验和代码课。这一周的内容相对简单，主要专注于为Agent准备的Skill Pack文件。我们不写新的业务功能，而是将前8周积累的工程能力封装成可被Agent发现、按需加载并由发布清单锁定的Skill Pack。 Skill Pack旨在解决项目复杂化时的问题，确保AI助手按照已定义的工程规范、脚本和模板工作，并加强安全边界。例如，第二周的数据契约输入控制面沉淀为data-contract-lint工具，用于检查数据契约、PII、许可证和质量门禁。 第三到第六周的内容沉淀为ingest-backfill-runbook，限制补数范围并记录证据。第八周的RAG孵化则沉淀为rag-contract-check和prompt-release，检查证据引用、响应契约及Prompt发布和回滚。第九周还提前加入了release-check，涵盖数据、索引、Prompt和Skill等方面。
凭证、证据等信息被绑定到统一发布清单中。架构中包含了前8周的SOP、工程规范和经验，这些内容被固化为Agent使用的Skill。Skill的主要核心是SKILL.md格式文件及其下的3个子文件。 接下来，我们将对Skill进行contract测试，以验证其编写是否正确。这包括name、description、version和not_for等字段的约束。skill-pack.schema.json文件专门用于检查Skill的规范性。 渐进式披露分为几个阶段。首先是只读元数据，Agent根据元数据进行推理，匹配适合当前任务的Skill。匹配完成后，Agent会拉取详细的Skill数据，如正文数据、模板生成或证据引用。 我们的Skill注册中心由skill_registry.py管理，它负责处理Skill的注册和管理。此外，还包含了一些针对OpenAI或MCP等平台的适配性接口。
理解微服务的类比，它也有注册中心，用于服务发现和服务匹配。接下来详细讲解Skill的接口，包括渐进式披露的过程：先拉取所有Skill，再获取特定名称Skill的详细信息。 从构建到作为注册中心进行发现或渐进式披露，再到最终的治理，整个过程形成了一条完整的链路。这涵盖了第九周关于Skill的宏观代码架构，希望大家先弄清楚整体结构。 Skill代码主要位于skills目录下，其中包含SKILL.md文件，这是第9周的入口文档，介绍了Skill是一个可移植的小包，包含发现元数据、操作说明、可执行的帮助程序等。
对于复杂的工程任务，不能仅依赖提示词。我们需要将经验沉淀为可复用的Skill Pack，并通过README.md定义其价值，将其视为工程资产。 以data-contract-lint为例，其结构包括SKILL.md负责说明和路由，scripts负责执行脚本，references包含深度上下文。该Skill用于检查数据契约、PII、许可证及提交质量。 ingest-backfill-runbook规划Replay或补数，确保幂等性与回滚；prompt-release规范Prompt版本发布与回滚；rag-contract-check则检查相关合约。这些Skill均来源于课程中的具体实践。
我们主要检查RAG响应中的citation、evidence_id、release_id和trace_id。RAG不仅回答问题，还能证明答案的来源。 关于release-check，计划在14周后进行。它用于检查数据索引、Prompt、Skill和Eval是否绑定完整。我们将所有Skill从课堂中提取出来，以data-contract-lint为例，详细讲解其核心Markdown文件。 Markdown文件上半部分是YAML frontmatter，从第1行到第29行，主要用于给Agent提供系统提示，常驻内存。name是唯一标识，description作为搜索元数据，决定搜索命中。 description描述了功能及其使用时机。not_for字段防止误触发，明确功能边界。inputs和outputs定义了输入输出参数。
既然能输入和输出，其依赖和验收方式包括inputs和outputs。激活技能后的正文是给Agent的操作步骤和安全边界，这是第二步骤Stage 2。我们的SKILL.md文件包含这些信息。 在scripts中，可以看到要执行的脚本及相关说明。Skill需要运行脚本，这在课堂上已详细讲解。reference部分引用了Agent检索所需的上下文，并提供了一个输出模板。 Skill还需要一个契约来检查规范性，作为质量门禁。这是第9周的质量门禁之一，确保Skill的编写符合标准。
没有它，我们的Skill目录只是约定俗成；有了它，我们就能进行工程化测试，确保每个Skill的格式合规。在企业工程中，文档本身也是一种数据结构，具有契约性质。 Skill Pack未来可能被Codex、Claude Code MCP客户端或内部平台读取，因此其name、version、description等字段不能随意填写。通过定义契约schema，我们将这些字段从建议性规范变为强制性要求，以保证质量。 契约头部包括$schema、title和必须字段如name、description、version。例如，name字段必须符合正则表达式，并且描述要匹配目录名称。这种模式类似于之前的data-contract，最终作为质量门禁使用。
请大家重点关注测试中包含的字段。接下来，我们来看一下注册中心的结构。它位于 service 下面的 tool_api 中的 app 下的 skills 文件夹内，其中包括 skill_registry。这是第9周的核心代码。 Skill 最重要的特性是渐进式披露，分为两个阶段。第一阶段是将所有 Skill 的元数据拉取到内存中。在 Agent 进行任务推理时，会扫描这些元数据，找到匹配的 Skill，然后通过 get_skill 接口拉取具体信息并执行相关操作。 Registry 主要提供 list_skill 和 get_skill 两个接口。list_skill 列出所有 Skill 的元数据，get_skill 则获取详细信息。这类似于微服务中的服务注册中心，用于服务发现和元数据拉取。大家可以查看注释以了解更多信息。
首先，发现并激活相关组件，然后按需加载脚本、references和assets。其中，frontmatter和SKILL_NAME需要遵循特定的规范。skill_meta和skill_detail被分层处理，确保结构清晰。 skill_meta包含name和description两个必填字段，以及其他非必填字段。这些字段必须满足契约要求。而skill_detail则包括元数据body，以及scripts、references和assets三部分。 我们的Pydantic模型用于校验name，并计算digest指纹。此外，discover、get_meta、get_skill和search等开放接口供外部使用。这些接口还支持openai_tool_exports功能。
我们将介绍OpenAI的function tool的激活描述导出适配的附加功能，以及MCP式的mcp_tool_exports。这是用于MCP类型的描述导出和read-only event mode的标注。大家可以详细查看代码。 Registry的主要功能是管理服务。配置好Registry后，我们通过routers中的skills.py文件来对外暴露HTTP API。这个文件将Registry服务封装成REST API接口，类似于Java中的service实现。 在FastAPI中，get_skills函数返回技能的metadata和渐进式披露的描述，如stage 1、stage 2、stage 3等。这层描述返回当前的skills count。
在代码中，skill.name加载了具体的Skill。data-contract Skill具体到name级别，详细信息被添加出去。下面的/skills/exports/OpenAI是OpenAI的导出接口，而MCP也有其导出接口，共有4个接口。 我们的注册表内路径通过环境变量进入系统。这里有一个tool_api的config，路径为workspace/skills，这是容器内的默认路径。Docker Compose将仓库中的skills目录以只读方式挂载进容器，因此tools API无需将技能包打进镜像即可读取最新的Skill configs。 Skill configs包括skill_registry.py、metric_registry.py和release_id，这些配置对服务接口至关重要。最终，我们通过main函数作为服务启动的总入口，新增的skill router也在此挂载。这就是FastAPI应用的入口点。
挂载的是 API v1，我们借此机会复习一下整个 tools API 的整体职责。它承载了整个工单工具，并模拟企业内部的工单系统，包括工单接口、KPI 工具、HITL 和审计功能。总体而言，它在企业级服务中扮演业务层或后台服务的角色。 接下来讲解我们的 infra 中 Docker 如何读取 Skill。Docker 会通过 tools API 将 skills 目录挂载到 /workspace/skills，并将 skill_registry.py 传递给服务，同时与环境变量进行映射。这样便完成了 Skill 的传递过程。
在发布过程中，contracts上的release部分涉及一个manifest schema文件。这个文件不仅用于运行时发现技能，还能将技能版本锁定到发布清单中。第九周我们在发布清单中增加了skill_id，以便产品发布时不仅能知道使用了哪些数据、索引和Prompt，还能明确Agent选择了哪套Skill Pack。 eval_summary和rollout_notes作为发布的证据。接下来进入测试环节，主要测试Skill Pack。这不仅仅是为了形式上的美观，而是为了防止有人添加不合规的Skill。通过单元测试，我们可以遍历整个skills文件夹下的内容，确保一切正常。
该过程需要遍历整个skills文件夹下的所有SKILL.md文件，检查frontmatter是否符合我们的schema，并验证目录名是否与SKILL.md中的name一致。同时，确保每个Skill包含not_for、outputs、scripts、references和assets的正确结构。 当前有5个白名单项目，还包括frontmatter的校验。大家可以进行破坏性测试以验证格式。课堂上我们将直接进行正反面测试。 此外，还有manifest-skill绑定测试以及集成测试。集成测试主要针对skill_registry，目的是验证注册表的行为是否符合渐进式披露的设计架构。
这部分代码只能返回元数据，不能返回主体内容，这是正确的。第二个测试 get_skill 需要按需加载一个 Skill 的完整内容。此外，还有 OpenAI 和 MCP 的导出测试。这些测试涉及旁路运行和 endpoints 测试。整个第九周的代码结构清晰，易于理解，大家可自行查看并进行破坏性测试。 接下来，老师将演示第九周的任务。首先，我们需要做一些预备工作，包括复制环境变量。然后检查我们的 Skill Pack 是否存在。
在进行下一步之前，我们需要确保基础条件已经满足。接下来，我们将测试Skill Pack中的contract，以验证其是否符合规范。这个测试主要是检查Skill Pack的形状是否正确。
大家看一下，这里显示的是三个pass，说明测试通过了。我们的Skill Pack实际上是一个Skill Registry，它位于集成下面，还有一个test week09。这个也通过了，有四个pass。虽然可能会有一些warning，但这些警告可能是由于旧的compose文件或过期组件引起的，可以忽略。 基本上，我们已经验收好了这两个contract。接下来，我们将测试Skill本身的问题。老师之前在课堂上提到过一个原则：不一定非要在大模型中测试，因为如果本地Python脚本能跑通，在大模型中也能运行成功。现在我给大家展示一下我们在本地能够运行的内容。首先查看data，以及其中的lint，并挂载契约文件，这些都是没有问题的。
状态是OK的。我再看一下，第二个文件也是可以的。接下来查看第三个文件，它需要我们的资产模板，从资产中做一个模板输出，这也是可行的。 大家可以在后面自行查看如何运行这些脚本。基本上，这些脚本都已准备好供大家使用。
现在我们最小可跑骨架的意义并不在于替代完整的生产系统，而是展示Skill Pack可以携带脚本、模板和检查逻辑。第九周的边界是显性执行，不包括远程自动破坏性测试。 接下来，我们来看如何启动接口及其外观，因为这是为Agent准备的。您可以使用浏览器查看，也可以选择Postman以获得更好的格式效果，或者在浏览器中安装JSON插件来格式化输出。首先检查健康度，这是第一步，如果这一步无法通过，后续步骤就无法进行。
我们来看一下127.0.0.1的/api/v1/skills，它表示当前的Skill元数据。其中count等于5，与我们的数据一致。每个元素都有描述信息，包括渐进式披露的各个阶段和release_id。 接下来是format元数据信息，包含name、description、version、path和owner等字段。data-contract部分则不再一一展开。 以RAG为例，查看其具体内容。其中包括digest、body（即正文）、scripts、reference和asset索引带release_id。
我模拟了加载元数据和正文的步骤。我们再来看导出接口 /skills/exports 的作用。它返回的是Skill激活的描述，而不是业务动作执行结果。例如，OpenAI的接口会返回function激活的描述，需要传递参数属性。 MCP的接口与此类似。假设没有相应技能时，我们会返回 no such skill 给Agent。这是为了确保不出现乱打的情况。这便是Registry接口的一个测试示例。整个实验相对简单，我们就先到这里。
