大家好，今天我们继续第13周的代码和实验课。这节课我们将基于第7周的知识块生成有版本、有证据的关系图，并将其接入第8周的RAG服务。最后，我们将使用第11周的评测思路来决定哪些题型适合GraphRAG。 需要注意的是，GraphRAG并不是万能的。对于重置密码、查找制度或字段等简单问题，我们仍然采用向量和全文检索。只有关系题、跨文档归纳题和多跳题才是图数据库的主场。 从宏观架构来看，最上游的数据已经处理成标准化的证据资产。在第13周，我们在这些资产上进行派生，以继承数据的版本、权限范围和引用能力。构建过程中，我们先定义schema，再进行抽取、实体对齐和社区聚合，最终以graph_release为单位写入PostgreSQL。查询时，简单题路由到hybrid search，关系题则进入graph检索。
我们将数据压缩成文件后，再交给RAG系统生成引用。这一过程由系统元数据控制，确保模型不会随意生成内容。最终，我们根据事实性、局部性和全局性的多跳分类进行A/B测试，而不是仅依赖一个总体平均数来评估性能。 首先，我们通过一个分类器classifier.py判断题目是否需要图示。在build阶段，schema和string的结合非常重要。week13的数据目录包含先验数据，这些数据将上游证据块与已审核模型结合。随后，pipeline层处理原始数据，其中graph目录下的代码用于构建GraphRAG。 schema.py和schema.yaml定义了图的类型系统和质量阈值。models目录展示了如何构建图的数据对象。此外，extract模块负责实体抽取。
首先，我们进行受schema约束的实体抽取。接着，通过alignment实现实体规范化，生成稳定的ID并进行隔离。 接下来是图的编排，由build负责。编排完成后，数据将存入store.py和PostgreSQL中，形成持久化的落库。 在服务层，我们构建图并进行实体抽取。检索时，先分类选择合适的图检索方式，然后通过store中的图访问协议和PostgreSQL实现查询。 最后，我们将RAG source graph整合到API中，实现协议化。整个过程包括生成、引用边界以及统一的RAG收敛。
这可能是第13周的测试，其中包括Evals文件夹。Evals用于评测，下面包含了A/B.py和test等文件。我们逐个查看这些文件，首先来看data文件夹中的数据初始化文件。 data文件夹包含的是第七周证据资产的可复现实验切片。每行数据携带chunk的身份、event_id、release和作用域等信息。第一行数据是JSON格式，包括chunk_id、event_id、data release_id、product name以及visibility scope等字段。 graph文件夹下的schema.yaml文件规定了图的边界和结构形状。这个文件定义了图的数据模型，确保图的结果是可重复的。
它是图的业务宪法，规定了允许的实体、边以及端点连接方式和质量预值。这是我们的schema first实体抽取的先验规则，所有内容都需严格定义，避免大模型随意创造实体类型和关系动词。 代码的第一行和第二行展示了实体类型的白名单及其关系类型和端点。此外，还设置了质量阈值。通过这种方式，我们限制了大模型的自由度，确保其在受控条件下进行实体和关系的抽取。 schema.py文件引用了schema.yaml中的数据结构，并执行加载和完整性校验。这有助于尽早发现配置错误，防止系统启动时因不存在的实体或端点而失败，从而阻止脏数据进入存储。 生产系统需要多层防线来保障数据质量。首先校验schema本身，接着是抽取结果，最后是持久化约束。这种多层次的校验机制可以有效防止错误发生。
大家可以具体查看实践细节，了解我们如何在 graph 中构建关系图的数据模型。这里定义了图流水线的中间语言，主要涉及输入对象 source_chunk 和对齐决策。最终生成的 GraphBuildResult 是整个过程的报告。 代码中关注的对象生命周期包括：source_chunk 表示上游事实，entity_mention 和 relation_mention 是抽取的候选集，alignment_decision 记录合并或隔离的原因。graph_entity 和 graph_relation 作为图中的边，构成可查询、可审查、可审计的发布候选。 extract 协议隔离了抽取引擎，Extractor 实体是可替换的协议，默认实现也在其中。通过这个协议，我们可以灵活地更换抽取引擎。
接下来，我们讨论置信度的防御，这是关键代码的一部分。无论使用大模型、规则引擎还是第三方的GraphRAG，都需要实现一个接口层。这种设计模式基于接口实现，使得更换抽取器时不会影响整个检索流程。 alignment.py文件解决同一个实体多种写法的问题，进行规范化和生成稳定ID。在数据对齐过程中，先进行NFKC转换，再casefold和标点清理，然后按照schema aliases和精确匹配及高分fuzzy匹配来自动合并数据。
我们设定的阈值高于0.96，且唯一的候选集落在0.88附近时，存在歧义。在这种情况下，我们选择进行隔离处理，避免将两个不同的客户或产品错误地合并在一起。这就是我们在课堂上讨论过的实体对齐问题，即将同名不同义或同义不同名的实体进行匹配。 接下来，我们来看社区聚合。在community.py中，我们将通过节点和边生成可供全局搜索使用的社区。课程中的实现采用了BFS确定性连通分量方法。代码第11行是社区入口，下面是确定性摘要，大致有两个结构。 我们的社区生成基于BFS连通分量，并按成员排序生成稳定的摘要。这种方法的好处是没有外部依赖，结果可重复验证。生产升级时，可以替换为LLM并实现版本化社区。但输入仍需治理后的实体边，输出保留社区ID、member_ids、event_ids及graph_release_id，确保证据和版本不丢失。 完成实体抽取、识别与统一后，我们需要进行整体编排。这是构建过程的一部分，用于计算总编排并将所有内容整合在一起。首先是总体入口，然后是下面的具体结构。
这个地方是用于验证输入身份的校验。我们有一个客户端，其代码如下所示。我们要从 build_graph 开始读取，这是我们的总入口。详细的设计实践请自行查看。 我们将构建图来进行抽取、实体对齐、设置句格和摘要。编排完成后，这些数据将存入数据库。store 负责事务性的写入，确保所有步骤在一个事务中完成。任何一步失败，整个 release 都不会变成 active。 同一个 release 和 index_release 可以安全地重跑，内容和上游的 index_release_id 改变时，旧的 graph_release_id 将无法复用。这部分是数据存储的脚本，请仔细查看。 既然是存储，我们需要构建第13周的数据结构。infra 中包含了一些 SQL 脚本，展示了如何从图中获取数据。请大家查看这些 SQL 脚本，特别是 graph_release 的部分。
Graph_evidence_projection 是所有图资产的父身份，它冻结了引用的文本和定位信息。节点、边、社区等都是外键挂在 release 下面。图的构建实体包括节点、边、别名、社区等主实体。 我们将这些图资产编排到 Dagster 的资产中，与之前的流程一脉相承。Dagster 负责编排和观察构图报告。第13周的 graph_release 也会发布到这里。 代码中先确定 release_id、graph_release_id 和 source_path，再执行 build。元数据记录 trunk_entity_indices、communities 和 stable status，方便在 Dagster UI 中查看运行结果。 我们先构建存储，然后进行编排，最后到查询。查询部分在 services 里面，专门有一个 graph 模块来处理。
这是所有查询的处理逻辑，首先是分类。这个分类作为整个查询的 graph route 的准入阀门，是一个判断型脚本。它决定问题应抽取给哪些链路。例如，全局共性内容放入 graph global，关系链和传播影响路径则根据具体情况进行路由。 查询模式包括 global、multi-hop、local 和 hybrid。分类器通过确定性规则保证可解释性：全局词命中走 global，多跳关系词命中走多跳，局部关系走 local。这些规则清晰地定义了不同查询模式的应用场景。 models 用于构建和运行时描述如何造图及用途。路由对象、graph path record 和最终结果是三个主要结构，用于描述如何使用图。这类似于 Java 代码中的 POJO，构建时可能是 DTO 或 DAO 对象。
这里介绍的是一个模型，输出时会生成一个POJO对象。需要注意的是，这两个对象可能并不相同，这主要是一个概念性的区分。 此外，还有一个store的概念。构建侧的store负责写入操作，而运行时的store则用于受控的读取。例如，GraphStore将find_entities、traverse和list commits等功能抽象出来。 这种设计是一种模式或防御性编程。我们定义了GraphStore协议，并提供了存储协议。在内存中测试适配器，然后是实际的适配器。目前使用PostgreSQL作为存储，未来如果需要更换为Neo4j，可以进行隔离而不影响代码重构。 最后，我们将讨论检索功能。通过上述条件的理解，我们可以更好地掌握检索这一部分的内容。
统一入口通过解耦器将所有边界条件囊括进来，并按照其检索逻辑进行检索。这些逻辑非常清晰，大家可以查看实际的细节。 检索出的内容需要转换为大模型可读的授权文本。为此，我们可能需要对路径和数据进行序列化，形成结构化信息。此外，还需对图查询结果进行元素过滤和优化，只序列化选中的路径和社区，并过滤出优秀的embedding_id集合。 在总体的source中，RAG API已经集成了GraphRAG的能力。大家可以查看总的RAG models部分，其中包括了graph_release和GraphRetrievalDebug等内容。
我们来探讨一下RAG模型的响应结构。在RAG.py中，我们通过service_run_api进行路由处理，并且可以看到RAG的具体实现。此外，我们还关注HTTP入口和引用构造，这些是生成答案的重要步骤。 接下来，我们将查看大模型生成的过程。从答案生成入口开始，经过置信度检查、提示加载等环节，最终完成输出。另外，我们在第13周的评估结果也将在下方展示。最后，我们还会介绍A/B测试的相关内容。
这里涉及样本的读取和分类比较。我们需要对四类不同的查询类型进行分类比较，而不是计算一个总的平均分数。这就是分类评估的优势所在。 接下来是一些测试代码，大家可以自行查看。我们将运行本周的一些实验。首先，我们需要检查环境是否已经配置好。如果环境变量已存在，则无需再次拷贝。随后，我们将启动服务并检查其是否正常运行。
服务已基本启动。我们现在开始进行第13周的增量迁移，为现有的PostgreSQL存储卷添加GraphRAG的相关表。 接下来，我们将通过特定方式执行之前准备的SQL脚本。这些脚本将导入到010位置，并模拟graph图的数据。 可以看到，所需的表已经成功创建。现在我们来查看具体有哪些数据被导入了系统中。
大家可以查看schema public。我们已经构建了包括隔离、社区、成员实体别名、实体节点、发布边和关系边等数据结构。现在我们将进行输入和schema的检查，运行devbox来查看我们的JSON文件，了解其包含的具体内容。 JSON文件中包含了非常结构化的信息，如track_id、event_id、document_id以及质量门禁等。接下来，我们将运行程序以检查实体、边、社区和隔离记录，但不会写入数据库，而是先使用Python模块化的方法进行验证。
这个地方还有问题，没有开启它的DevOps，少了个参数。可以看到我们的成功已经通过了，source的graph_release也是有的。实体大约有16个，边大约14个，社区大约两个。这些数据显示一切正常。 我们也可以将这些数据持久化到PostgreSQL中。这是真实执行的数据。刚才的内容，我们需要添加一些参数进行持久化。比如release_id和index_rel，设置为week08，并指定obs。这里可能之前已经执行过。
我们使用不同的V2执行成功后，现在来检查洗库情况。首先查询graph list中的数据，可以看到有4行记录，其中包括V2和V1的数据，且都是active状态。 接下来，我们查看V2刚刚执行过的实体节点。通过修改查询条件为V2，进一步确认其具体信息。
这里可能还缺少一个数据框，大家可以查看一下。这可能是之前的问题，现在已改为V2版本。可以看到，product的数据应该是正确的，实体数量也得到了确认。 接下来我们看一下关系边的具体情况，并顺带修改相关参数。传入条件也更新为V2版本。现在这些内容已经写入完毕。 最后，我们来查看社区的一些聚合信息。社区的相关数据也已经准备好了。
我们已经验证了存储，现在来看看如何验证多跳推理。我们可以构建一个查询，进入RAG并提出关于North Star Workspace的SSO登录问题及其症状和解决方案的关系链。这是一个多跳推理的例子。 答案显示，这一过程不是在内存中拼接数据，而是直接连接PostgreSQL数据库找到种子实体，并执行三跳推理。大家可以查看具体的跳数，例如通过事件ID获取证据，验证路由边界。 接下来，我们看看过去半年故障的所有共性，这是一份社区摘要或聚合图。我们将展示它如何进行路由，其类型是全局性的。
我们首先通过一个简单的直接查询来演示如何使用混合查询进行密码重置。这个过程没有问题，接下来我们将详细讲解A/B测试的门禁设置。 我们将为大家详细介绍质量门禁A/B测试，包括使用eval和GraphRAG的A/B测试方法。这些测试将应用于我们的数据案例中。此外，我们还会查看向量和图RAG的状态，确认其是否通过。
这里展示了一系列数据，包括factual、global、local以及多跳等具体数据。接着，我们运行第13周全套容器的测试，查看test目录下的contract文件等内容。
我们已经完成了17个任务，接下来在Dagster中查看如何处理图量化的资产。启动Dagster后，可以看到其界面。 现在，我们将图产物进行物化。命名完成后，执行命令行以物化两个资产。
我们已经定义了两个资产，并成功运行。现在可以看到，这两个资产脚本（graph_release和graph_build）已经物化成功。它们已融入Dagster系统中，且兼容性良好。 接下来进行安全演示，包括降级与发布不可发布的操作。我们将通过命令行验证过去半年所有故障的共性问题。
它是可以降级的。请注意，它没有结合到结果中，表明它已经降级了。我们来查看一下是否有错误类型，并使用V3 release_id测试是否会失败。
可能是让它重新运行，或者使用V2版本，这基本上和验证其密等性相同。它已经验证通过，我们不需要再构建新的图ID，这是没有问题的。今天的实验课就到这里，感谢大家的时间。
