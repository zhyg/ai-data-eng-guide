大家好，今天我们继续进行第4周的实验课。在IDE中，相关代码位于pipelines下的lakehouse文件夹内。主要文件包括assets.py、catalog.py、demo_schema_evolution.py等，请先浏览这些文件以形成初步印象。 第4周的任务是将第3周已规范化并存入PostgreSQL的数据物化为Iceberg表。这样做的目的是使数据具备快照时间旅行、schema evolution和可复现的表状态。简而言之，就是将业务系统中的当前账本通过Iceberg转化为数据湖中的版本化档案柜，每次写入都会留下一个快照，便于后续追溯历史版本。
我们有文档类型的表和工单类型的表，主要关注的是 ticket_fact 和 document 类型的表，如 knowledge_doc、raw_doc_asset 等。这些原始表大约有 50 条记录，包括工单表和 ticket_fact 表。 在课程中，我们至少讨论了四个表。这周我们将重点关注这四个表。文档以 log 格式存储，包含四条记录。我们需要将 PostgreSQL 中的这些原始表通过入湖操作进行处理。 入湖操作的第一步是读取连接参数，然后使用这些参数创建 Iceberg 表。这个表代表我们的 catalog 文件。
首先，我们创建meta指针文件和namespace tables。通过物化脚本查询PostgreSQL并将数据写入Iceberg。写入后，进行一系列测试，包括检查元数据、snapshots、files、metadata log和history这四个对象。接下来，我们将进行time_travel和schema_evolution测试，这是课程的两条主线。最后，形成baseline report。 接着，我们详细理解代码，从settings开始。settings是一系列配置文件，其核心作用是将Lakehouse的相关环境变量集中到LakehouseSettings中。例如，catalog_name、type、uri和warehouse等环境变量被写死在这里。测试时，可以通过配置方式或os.getenv获取这些变量。
许多人支持这两种方式。我们需要记住一个环境变量，并通过一个测试命令来验证其有效性。需要注意的是，PostgreSQL 和 Minio 并不在 localhost 上，而是在 DevBox 容器中通过 Docker Compose 服务名称进行通信。 关键代码中包含了一个检查功能，用于验证环境的一致性。例如，URI 必须是 PostgreSQL 的，而 Warehouse 必须以 S3 开头。这些规则都是实际约定的一部分。如果不符合这些规定，检查将不会通过。 接下来我们来看 iceberg_schemas 的作用。它实际上是通过 Python 字典来描述三层表结构。
我们先关注Bronze和Silver层，因为这两层是面向数据消费端的。课程中明确指出，我们支持的最小四张表包括：Bronze层的raw_ticket_event和raw_doc_asset，以及Silver层的ticket_fact和knowledge_doc。 以raw_ticket_event为例，它是一个字典结构，其中嵌套了字段和分区spec作为隐藏分区，并包含排序字段。这个文件定义了表的结构，为我们构建了一个框架。 通过catalog.py文件使用PyIceberg API来创建Iceberg表。这涉及到引入设置和之前提到的两层Schemas，结合这些信息进行pyarrow表的建设。核心表是上述四个表，Schema来源于这些表。
在建表过程中，我们有一些选项，比如冒烟测试或确保 JSON。此外，还可以进行预演。冒烟测试可能包含一些检查项。接下来介绍四种主要方式：第一种是通过调用 pyiceberg 方法来加载 lakehouse_catalog；第二种是使用 boto3 确保 Minio bucket 存在；第三种是创建 bronze 和 silver 命名空间；最后一种是将 Iceberg 的 Schema 转换为 PyArrow 格式的内存字段。 ensure_core_tables 函数用于创建表，这是最核心的建表模式。代码通过循环实现。后面我们将演示冒烟测试的具体过程。
冒烟测试涵盖了整个逻辑，包括 bucket Catalog、namespaces 和 table 的检查。我们在建好表后，会进行物化操作，即将 PostgreSQL 中的源表读取并转换为 Arrow 格式，再通过 overwrite 写入 Iceberg 表中，这些操作均使用 Iceberg 和 PyIceberg 完成。 最核心的文件是 TABLE_QUERIES，它定义了从 PostgreSQL 表中查询数据以填充四大核心 Iceberg 表的方法。例如，使用 distinct on 从 PostgreSQL 查询数据并写入 Iceberg 表。此外，fetch_source_records 通过异步连接方式查询 PostgreSQL，而 rows_to_arrow 则将列表中的历史质检数据转换为 pa.table 格式。
pa.table 和物化 tables 确保了表的存在并读取 source。重点在于读取后写入的逻辑，以及为不同层写入不同的模式标签。 inspect_metadata.py 用于查看 Iceberg 的元数据，通过 API 方式查询 snapshots 和 history，并支持原始方式和 metadata log。课程中详细讲解了这些内容，请大家仔细学习。 第一个实验是 time travel，通过 Iceberg 的 snapshot ID 读取历史版本，对比当前行数与指定 snapshot 的行数变化。这有助于更直观地感受 time travel 功能。 接下来将介绍 schema 演化的脚本，具体内容将在后续部分详细讲解。
Iceberg允许安全地新增列。通过对比新增列前后的Schema进化，可以检查数据能否安全插入。我们生成了一个基线报告，以便大家查看整个过程。 我们的代码包括一些Markdown文件和assets.py。本周不进行设计，但会在第6周练习中使用这些文件，因为它们与Dagster资产编排相关。在运行代码之前，我们需要先检查环境是否准备好。
可以看到，这个项目已经在我们的目录下了。请检查一下我们的分支是否正确。接下来，我们需要操作环境迁移，进行本地环境搭建和配置。 确认无误后，我们启动服务。这个过程应该可以手工顺利完成。服务运行后，后台也会相应启动。这周我们将重点关注几个关键部分，比如 PostgreSQL 数据库，因为其他组件可能会占用资源并产生干扰。
我们已经完成了之前的步骤，现在看起来没有问题。接下来，我们将进行一些环境上的冒烟测试。首先，我将运行一个devbox，并在Pipelines中执行Lakehouse模块的setting部分，以确保环境变量设置正确。 通过命令行概念进行check，确认环境变量已成功设置。之后，我们将再次运行冒烟测试，进一步验证Catalog的相关功能。
通过执行Catalog命令进行冒烟测试，可以确认表尚未创建。这表明表能够被成功创建，并且会生成4个最小表，符合课程内容。大家可以查看catalog中的omni、bucket状态以及两个命名空间：bronze和silver层的表。 登录后，可以在Lakehouse层下的Warehouse目录中找到bronze.db和silver.db数据库。这些表的存在意味着后续步骤中会有相应的数据表。每个表包含data和元数据，符合Iceberg的目录结构。 Data部分由parquet文件组成，而元数据则以JSON文件形式存储。通常情况下，元数据文件的数量会多于数据文件。
Iceberg采用目录树结构，这与我们的架构相吻合。元数据和数据文件都存储在Minio中，而PostgreSQL则用于存储catalog。通过查看PostgreSQL中的内容，可以看到它保存了Iceberg的namespace以及空间表等信息。 在这些记录中，我们可以找到元数据指针，而非表指针。当前元数据的位置也一目了然。Iceberg的catalog metadata、data三者之间的关系十分明显。 接下来，我们将基于前面的代码进行完整的测试。现在开始运行物化过程。
考虑到老年人视力可能不佳，compose文件在同一目录下直接使用。我们先进行dry-run以确保不会污染生产环境。此操作非常慎重，以免造成环境污染。 dry-run的目的是将PostgreSQL中的表数据翻转过来。可以看到，50条数据和4条数据都能顺利通过。接下来，我们将实际运行这一过程，并记录下操作步骤，这是我们的工程规范之一。
它是通过实际运行来展示的。你可以看到上面的action都是统一的dry_run。overwrite是实际写入操作，返回的内容包括release id和batch id。写入模式是可重复的refresh，并以overwrite方式执行。每个表的输出包括source_rows、write_mode和action。真实写入报告还包括生成时间、tables、source_rows、action和overwrite等信息。这些输出既可以帮助我们查看过程，也可以检查结果。 跑完之后，我们再来看一下Iceberg的元数据。通过py调用Iceberg的元数据，我们可以检查实际情况是否与我们的预期一致。
我们还是复制原来的命令，但这次可能不是同一个模块。可能是使用另一个名为 inspect 的 Python 文件。 我们将使用一个表，名为 Silver ticket_fact，来查看其所有快照的情况。可以看到它在特定时间点的数据变化情况。 该表有 7 个快照，记录了 ID、父 ID 和序列号的变化，以及变化的时间点。接下来，我们进入具体内容查看详细信息。
大家可以看到，这个时间是在2026年4月27日。接下来，我们再看最新一版，这是5月6日23点的版本。这表明它能够进行时间回溯，不依赖于我的快照。 现在我们来看看它的历史记录，这里展示了四个对象的变化时间轴，是从快照中提取出来的。具体来说，ID和时间序列是从刚才的数据中挑选出来的。接着，我们来看一下这些文件对应的版本。 这些文件实际上对应到一个Parquet文件。因为数据量不大，可能没有进行分层处理。
没有做分区的情况下，PARQUET格式的50条数据可能会导致count重复写入。文件大小和元数据变更情况如前所示。元数据的演进速度通常比数据本身快，因此大部分变化体现在元数据上。 接下来我们进行时间旅行实验。刚才查看的是23点的数据，现在我们将从02号开始再跑一次真实线上的数据，并检查snapshot是否有变化。
这说明它会新增一个 snapshot 版本。我们看最终的这个版本，之前的 snapshot 序列号是 7，现在的时间是 23:15。最新的版本显示时间为 11:24，这意味着时间已经发生了变化，并且新增了一个状态变化。 今天的内容说明了 snapshot 能够起到记录状态变化的作用。接下来我们将演示如何实现 time travel 功能。time travel demo 主要展示的是 table silver 的相关操作。
我们来看一下 silver 工单下的 fact，以及如何将其输出到 report week04。我们将使用 time travel demo md 的 01 来进行操作。报告输出包含 snapshot_count 和 selected_snapshot_id，最新的当前行数为 50，这符合我们的预期。 通过上述数据，我们可以验证 time travel 功能的有效性。接下来，我们再看一下 Schema 的变化。
比如，我们在某个表中新增一个列，以铜层的表为例。然后我们尝试输出结果，但遇到了问题，可能是模块未找到，请仔细检查。
请注意，schema已被正确修改。对比增加前后的状态，可以看到新增部分已经添加成功。接下来，我们将展示如何在lakehouse上生成baseline_report。
这份报告非常详细，列出了所有表格以及行数、快照数和文件数等信息。这些数据对我们后期复盘很有帮助。此外，报告采用了固定格式，便于阅读和理解。 至此，我们已经完成了这一周的时间课和代码课。大家可以在课后深入研究 Iceberg 的深层结构，并仔细参悟。如有任何问题，请随时向我提问。
