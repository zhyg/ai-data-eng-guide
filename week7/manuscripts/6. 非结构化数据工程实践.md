大家好，今天我们进入第七周的代码与实践课。本周的核心是将企业中的非结构化资产转化为可治理的数据资产。我们将解决四个主要问题：一是资产来源及其验证；二是不同模态的解析能力；三是如何追溯原文页面时间戳或对象；四是确保处理后的数据能够安全地用于第八周的检索、重排等用途。 首先，我们来看一下整个第七周的代码结构。这个结构非常清晰，展示了非结构化数据资产的处理流程，包括构建数据资产清单，通过脚本进行验证，并最终获得源文档。这些步骤为我们第八周的准备奠定了基础。
在源文档之后，通过一个智能解析器（如IDP）进行自动化解析。解析完成后，进入跟踪阶段，将数据切块并设置证据锚点。接着，通过连接器和网关，最终使用运行包裹将整个过程串联起来。 我们的数据资产主要包括四种类型：PDF图像、音频、视频和文本。这些数据存储在特定的媒体文件夹中，包括JSON分析数据、WAV音频结构数据、文本数据、MP4视频数据以及PNG图片和非结构化PDF文本等。这些都是我们处理的非结构化数据。
它包括检查校验和、输出类型等输入边界，这与我们之前讲解的内容一致。在加载过程中，系统会读取本地文件或输入路径，将其转换为文档，并进行一些校验，如校验和和源指纹。最终输出的是一个结构化的文档。 该文档将通过一个多模态解析引擎进行处理。对于不同的资产类型，引擎采用不同的解析方式。例如，PDF 文件优先使用特定的标记方法，其次是其他技术；图像文件则通过 OCR 处理；音视频文件有各自的转录流程。这些步骤确保了内容的有效解析。 解析后，输出的是带有回溯信息和存储柜记录的数据段。后续通过结构化感知的方法对数据进行切分，以便进一步处理。详细的代码实现可以在后面查看。
架构图描述得比较清晰，大家可以对照查看代码，并利用AI辅助能力进行代码走查。 接下来是证据锚点的生成，为每个串生成一个锚点。例如，PDF需要在配置对象中设置锚点，图片需在特定对象中设置，音视频则需在时间戳和关键帧中设置。这些锚点记录了元信息指纹，包括页码、时间戳、pass the cabinet和release ID等关键信息。 之后，我们将进行质量检测，如CICD，以确保串改锚点和PDF配置等正确无误。通过我们的系统将这些信息串联起来，并写入库中。这就是整个代码架构的概览。 代码中的资产目录位于data文件夹下，具体在多克斯下的第七周文件夹内。
这是我们的代码结构。首先来看一下c_many_face文件，它位于data目录下的c_the_many_face文件夹中。大家可以参考第七周的内容。这个文件包含了south ID、CA sip type、chick sam check sum 256、PI状态、契约引用以及set car路径等字段，这些信息都写得比较清晰，符合我们之前的规范。 south ID用于后续的track和证据锚点追溯到具体的资产。sip type决定了parcel adapter的路由方式。check sum 256确保了处理的原始文件和声明的一致性。对于音视频，还会有transcript object pass；而对于图片类，可能会有OCR test pass。
视频中还包含关键帧，例如k free me OCR pass。在企业实践中，通常不会直接使用原始文件，而是提供一些上游的SSR、OCR或VLM产物。这些半成品经过大模型或其他小模型处理后，再将数据传递给我们进行业务处理。 此外，还需要进行PI操作，这决定了数据是否能进入索引的全链路。这与之前的规划是一致的。 最后，我们来看一下low logo paton logo的牌子，这个不能关闭。拍摄是在拍卖栏内进行的。
这个功能将本地或many faced的输入路径转换为saws路径，不解析具体内容，仅确认文件是否存在，并读取内容计算其指纹。它不会执行复杂的业务逻辑，如文件不存在时，会根据配置产生警告或强制处理。 质量门禁根据校验结果阻止脏数据进入后续流程。例如，如果输入不可信，会在入口处暴露问题。文档、跟踪和锚点等所有部分的源头都需要通过这一关卡进行质量控制。 接下来讨论自然路由方式，如PDF和IDP图像、OS2音视频等。尽管代码量较多，但自动路由可以简化处理过程。
这段代码的结构相对合理。课程中已涵盖相关流程处理。完成扒手操作后，产品通常需要外部力量进行处理，例如结构化感知等。结构化感知主要涉及数据结构，而context则通过串口提取重要字段，并辅以策略串联。 证据锚点是第七周内容的核心。它确保模型引用资料时，系统能够识别资料来源的具体位置和时间戳。在生产环境中，这是一项必须遵守的形式。大家可以通过models引入这些功能，详细查看相关内容。
在PDF中，常见的配置字段包括对象配置、音频视频等。这些字段非常重要，因为它们将在后续的证据引用中发挥作用，必须在这一周内固定下来。 接下来是quality kit部分，这是进入第八周的一个门槛。其中包含了大量的质量检测内容，大家可以详细查看。这部分会总结前面的结论，揭示存在的质量问题。 最后，有一个run部分，将所有文件串联起来形成一个流程。这个流程比较庞大，大家可以根据自己的AI能力进行深入研究。至于代码部分，我将不再赘述，直接进入实验部分。老师这边开了一个新的work tree，与GitHub上的最新版本一致。
在开始运行之前，我们需要做一些常规操作，确保环境配置正确，以防止后续出现问题。确认无误后，我们启动服务，并让它在后台运行。
现在我们开始进行合同测试，也就是契约测试。这些测试位于test目录下，其中包括一系列的契约测试，如schema future name等。大家可以看到，这主要是一些单元测试模式。 我们将使用这些测试来进一步说明。需要注意的是，对于其中的一些组件引用，大家可以不必过多关注。
可能是字符上的一个问题，实际上测试耳朵已经通过。至于小区内具体测试的内容，大家可以课后自行研究。课堂上可能不会讲得非常详细，但我会给大家指出一些关键部分。 测试的目的在于规划好我们的输出，确保其满足特定的约束要求。例如，缺少锚点或PDF配置的问题应在契约成长阶段暴露出来。然后我们会运行一个大型测试，涵盖整个流程。 我们正在开启一个临时的虚拟化容器。如果是DV box，我们将重新使用它来运行模块化的东西，比如many fast的测试。
它有一个参数，即PRSE，这是一种自动化方式。此外，还需要选择一个串口策略。这些设置是章节感知的，具体策略按章节切分PDF或数据。 贝塔贝塔瑞历史IDLEASEIDID是一个重要的trace字段，用于后续跟踪。我们还可以添加MULTI码、时间戳（如2026619）和时间（如11）。 在运行时，如果去掉转让方式，可能会导致真实数据入库。请大家在操作时注意这一点。
为了避免产生脏数据，我们先查看效果而不直接修改数据库。这是公司的守则之一，因为处理脏数据的成本可能远高于预防成本。 这是我们生成摘要的目录结构。接下来，我们将创建一个更详细的 Passle 报告，并为第八周的任务准备相应的参数。
参数已基本输入完毕，大家请查看效果。首次尝试可能因数量过多而出现问题。当前状态为wall，至少没有失败。接下来，我们详细查看结构。 首先看manifest pass，它指向manifest的路径。我们在代码中提到过，这是第七周的一个节省文件。passo的作用是让adapter按照asset type顺序路由。 trunk strategy通过结构杆子进行track。这是我选择的一种track策略。
大家可以尝试一下这个策略。我们还有一个重要的概念是Release ID，它用于后续的跟踪和信任跟踪。 在正式入库前，我们需要运行一次测试，这类似于第八周的质量本金内容。如果PDF中没有复杂的依赖或OCR提示，那么我们的测试就算通过了。 既然命令成功执行，我们来看一下具体的报告内容。我们生成的202611文件请大家查看，这是质量检测的一部分。
在 Parcel Run Report 中，首先可以看到状态为 morning。接着是 bh ID 和 parcel 类型等信息，其中 string version 是结构化感知的。此外，还包括 data 60ID 等常规原子段。 报告将原来的 SaaS count 切分为七个 track count，并详细记录了摘要文件。这些摘要文件进一步细分，以便更好地进行后续排名。第八周的门禁质检报告也包含在内，提供了更详细的记录。 为了验证结果，我们还运行了一个真实的数据库。这部分的具体内容就不在此展示。
我将直接使用前面的方式运行。首先，复制一个命令过来。我们需要修改一些内容，首先是我们的 ID，改为 release ID112。其他部分保持不变。可以看到，我已经去掉了 Report 参数。现在我们再次运行，这次成功了。 接下来，我们需要记录这个结果，以便后续作为数据库查询的组件。现在可以开始查询。如果你安装了 DataGrape，请检查是否已经安装。
我们来看一下，可能会遇到一些问题。这个年龄的数据可能存在问题，但确实有数据。我们刚运行了一个ID，这个ID是有数据的。现在给大家展示一下，看看是否可以格式化。 这段代码相对简单，我们选择了几个字的计数。先执行一部分代码，然后再继续。总的来说，这段代码其实很简单。
我们来检查证据锚点的进展情况，这涉及到整个链路的审查。我们需要查看解析部分，并确认在锚点之后是否有我们的 section。这些 section 包括各种类型的音视频资料。 接下来，我们将综合分析 section 的内容，包括一些资料和归纳信息等。这样可以更全面地解释相关数据。
这说明我们成功运行了数据库。大家可以观察一个问题，即我们需要的条件可能在多次查找中有所不同。有些是 set card，有些是我们自己跑的。大家可对此进行研究，这里有一些细节可能老师没有详细讲解。这也证明我们已经完成了整个流程。 注意，我们之前提到的表可能是一份文档级的记录，而这里是传感器的实际记录。接下来，我们将按照惯例进行全面的回归测试，再次运行所有测试。由于前面已经演示过，我将直接进行整体测试。
基本上已经全部通过了。接下来，我将查看资产编排中的定义方式。这里不再打开相关界面，而是直接使用拍摄代码进行演示。让我们看看它是如何实现的。 Dfs，对，又搞错了，是dv。可能是品牌手机的S让我产生了误解。
这个格式可能有些混乱，但它实际上是在展示低于我们资产 test 的内容。如果有剩余，大家可以在我们的用户界面上查看。老师通过这个定义了资产 SIICT care。大家可以自行在界面上查看，我就不详细说了。 这里再提一个问题：如果我们本地随便创建了一个文件，该怎么处理呢？可以尝试假设我们创建了这样一个文件。这个问题我们在之前的课程中已经讲解过很多次，大家可以回顾一下。
在我们的家乡，使用刚才的脚本来查看一个HTML文件。首先，输入一个路径下的文件作为input path，然后使用上市ID和HTML类型。大家可以查看代码中的passel sport back部分。 我们将release ID设置为20260609，并选择一种转让方式。这个目录的具体内容可以查看。我明白了其中的原因。
大家可以看到，我们可以通过自定义输入和本地文件来运行整个流程。这表明我们的链路并不完全依赖于固定的路径，而是可以灵活复用的。经过puzzle track、锚点、call gate等步骤，我们可以自由配置，不依赖原有的固有路径。 实际上，非结构化数据与结构化数据在处理上具有同等重要的地位。我们在第二周到第六周完成了结构化数据的处理，而将非结构化数据处理压缩到了一周内完成。
从第二周到第六周我们打下了基础，这部分内容80%可以应用于数据分析。实验中不会重复讲解这些基础知识，因为那会耗费大量时间。 实验将重点关注结构化和非结构化数据处理的不同之处。一些具体步骤如契约、入壶及对象存储等，请大家自行尝试。如果有任何疑问，欢迎在群内讨论，我会尽力帮助大家理解。 今天的实验课就到这里，感谢大家的时间。如果遇到问题，希望大家积极交流，我会尽力提供帮助。
